<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="row">
	<div class="form-group">
		<%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
			<%= f.submit "Create Account", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>
